# CloudSpot Web App Prototype

Working out the responsiveness and UX kinks that migrating to a native web app would entail.

`npm install`

then

`npm run dev`

(and possibly `npm run build` at some point in there too. I dunno. I just work here...)
